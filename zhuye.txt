import React, { useState, useEffect } from 'react';
import { Bell, Play, Home, Search, Library, PlusSquare, Disc3, Cast, Settings, MessageSquare, Moon, Timer, LogOut, ChevronRight, User as UserIcon } from 'lucide-react';

const App = () => {
  const [feedData, setFeedData] = useState([]);
  const [isLoading, setIsLoading] = useState(true);
  const [isDrawerOpen, setIsDrawerOpen] = useState(false); // 新增：控制侧边栏状态

  // 模拟从网易云 API 获取个性化数据流，但渲染为第一版设计好的 UI 样式
  useEffect(() => {
    const fetchPersonalizedFeed = async () => {
      try {
        setIsLoading(true);
        // ==========================================
        // 真实 API 调用示例 (对接网易云接口时使用)
        // ==========================================
        /*
        const response = await fetch('http://localhost:3000/homepage/block/page');
        const data = await response.json();
        // 此处需要将网易云的 blocks 数据格式化为我们 UI 引擎支持的 type
        const formattedBlocks = formatNeteaseApiData(data.data.blocks);
        setFeedData(formattedBlocks);
        */

        // 模拟网络延迟和 API 返回的动态数据结构
        setTimeout(() => {
          setFeedData([
            {
              id: 'recommendations',
              title: '为你推荐',
              type: 'large-card-carousel', // 对应第一版大方块卡片
              items: [
                { id: 1, title: '每日歌曲推荐', subtitle: '根据你的红心歌单生成', image: 'https://images.unsplash.com/photo-1493225457124-a1a2a5956093?w=500&q=80' },
                { id: 2, title: '私人FM', subtitle: '最懂你的专属频率', image: 'https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=500&q=80' },
                { id: 3, title: 'Taylor Swift 1989', subtitle: 'Taylor Swift', image: 'https://images.unsplash.com/photo-1614613535308-eb5fbd3d2c17?w=500&q=80' }
              ]
            },
            {
              id: 'favourite-artists',
              title: '关注的歌手',
              type: 'circular-carousel', // 对应第一版圆形歌手卡片
              items: [
                { id: 4, name: '周杰伦', image: 'https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?w=200&q=80' },
                { id: 5, name: 'Taylor Swift', image: 'https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=200&q=80' },
                { id: 6, name: '陈奕迅', image: 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=200&q=80' },
                { id: 7, name: '邓紫棋', image: 'https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=200&q=80' },
              ]
            },
            {
              id: 'your-radar',
              title: '你的私人雷达',
              type: 'large-card-carousel', // 动态增加的区块，自动复用UI
              items: [
                { id: 8, title: '华语流行', subtitle: '捕捉你的专属频率', image: 'https://images.unsplash.com/photo-1518609878373-06d740f60d8b?w=400&q=80' },
                { id: 9, title: '深夜EMO', subtitle: '网易云音乐推荐', image: 'https://images.unsplash.com/photo-1516280440502-86927dcc3ed3?w=400&q=80' },
              ]
            }
          ]);
          setIsLoading(false);
        }, 800);
      } catch (error) {
        console.error("哥哥，获取数据失败了哦！", error);
        setIsLoading(false);
      }
    };

    fetchPersonalizedFeed();
  }, []);

  // 动态渲染区块内容的引擎 (保留了第一版的经典UI风格)
  const renderSectionContent = (section) => {
    switch (section.type) {
      case 'large-card-carousel':
        return (
          <div className="flex gap-4 overflow-x-auto px-5 no-scrollbar pb-4 snap-x snap-mandatory">
            {section.items.map((item) => (
              <div key={item.id} className="flex-none w-40 flex flex-col snap-start group cursor-pointer">
                <div className="w-40 h-40 rounded-2xl overflow-hidden mb-3 relative shadow-lg">
                  <img 
                    src={item.image} 
                    alt={item.title} 
                    className="w-full h-full object-cover group-hover:scale-105 transition duration-300"
                  />
                  <div className="absolute inset-0 bg-black/20 group-hover:bg-transparent transition" />
                </div>
                <h4 className="font-semibold text-base truncate">{item.title}</h4>
                <p className="text-sm text-gray-400 truncate">{item.subtitle}</p>
              </div>
            ))}
          </div>
        );
      case 'circular-carousel':
        return (
          <div className="flex gap-5 overflow-x-auto px-5 no-scrollbar pb-4 snap-x snap-mandatory">
            {section.items.map((artist) => (
              <div key={artist.id || artist.name} className="flex-none flex flex-col items-center snap-start group cursor-pointer">
                <div className="w-24 h-24 rounded-full overflow-hidden mb-3 shadow-lg">
                  <img 
                    src={artist.image} 
                    alt={artist.name} 
                    className="w-full h-full object-cover group-hover:scale-110 transition duration-300"
                  />
                </div>
                <p className="text-sm font-medium">{artist.name}</p>
              </div>
            ))}
          </div>
        );
      default:
        return null;
    }
  };

  return (
    // 主容器：恢复第一版适配网易云暗黑模式的深色渐变背景
    <div className="flex justify-center items-center min-h-screen bg-gray-900">
      {/* 模拟手机屏幕 (底层作为抽屉背景) */}
      <div className="w-full max-w-md h-[850px] bg-[#1a1a1a] text-white overflow-hidden relative shadow-2xl sm:rounded-[2.5rem] sm:border-[8px] border-gray-900">

        {/* --- 新增底层：侧滑抽屉 (Drawer) 面板 --- */}
        <div className="absolute inset-0 z-0 flex flex-col w-[75%]">
          {/* 抽屉头部 - 个性化音乐信息 (对接 /user/detail & /user/record) */}
          <div className="pt-16 px-6 pb-2">
            <div className="w-16 h-16 rounded-full border-2 border-red-500 p-0.5 relative mb-3">
              <img 
                src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=200&q=80" 
                alt="Profile" 
                className="w-full h-full rounded-full object-cover"
              />
              {/* 模拟黑胶VIP标识 */}
              <div className="absolute -bottom-1 -right-1 bg-gradient-to-r from-gray-200 to-yellow-500 text-black text-[10px] font-bold px-1.5 py-0.5 rounded-full border border-black">
                VIP
              </div>
            </div>
            <div className="flex items-center gap-2 mb-1">
              <h2 className="text-2xl font-bold text-white">哥哥</h2>
              <span className="bg-white/10 text-white text-[10px] px-2 py-0.5 rounded-sm font-mono">Lv.8</span>
            </div>
            
            {/* 移除社交属性，替换为音乐个性化数据 */}
            <div className="grid grid-cols-2 gap-3 text-sm mt-5">
              <div className="bg-white/5 p-3 rounded-xl border border-white/5">
                <p className="text-gray-400 text-[10px] mb-1">累计听歌</p>
                <p className="font-bold text-lg leading-none">12,450 <span className="text-[10px] font-normal text-gray-500">首</span></p>
              </div>
              <div className="bg-white/5 p-3 rounded-xl border border-white/5">
                <p className="text-gray-400 text-[10px] mb-1">云村村龄</p>
                <p className="font-bold text-lg leading-none">1,423 <span className="text-[10px] font-normal text-gray-500">天</span></p>
              </div>
            </div>

            <div className="mt-3 bg-white/5 px-4 py-3 rounded-xl flex items-center justify-between border border-white/5 cursor-pointer hover:bg-white/10 transition">
              <div>
                <p className="text-gray-400 text-[10px] mb-1">偏好曲风</p>
                <p className="font-bold text-xs text-white">华语流行 / 日系摇滚</p>
              </div>
              <ChevronRight className="w-4 h-4 text-gray-500" />
            </div>
          </div>

          {/* 抽屉菜单项 - 滚动区域 */}
          <div className="flex-1 overflow-y-auto no-scrollbar py-2 mt-2">
            <div className="px-4 py-2 border-b border-white/5">
              <button className="w-full flex items-center gap-3 py-3 px-2 rounded-lg hover:bg-white/5 transition text-gray-300 hover:text-white">
                <MessageSquare className="w-5 h-5" />
                <span className="text-sm font-medium">消息中心</span>
              </button>
              <button className="w-full flex items-center gap-3 py-3 px-2 rounded-lg hover:bg-white/5 transition text-gray-300 hover:text-white">
                <UserIcon className="w-5 h-5" />
                <span className="text-sm font-medium">我的音乐DNA</span>
              </button>
            </div>

            {/* 设置与辅助功能组 */}
            <div className="px-4 py-2">
              <button className="w-full flex items-center gap-3 py-3 px-2 rounded-lg hover:bg-white/5 transition text-gray-300 hover:text-white">
                <Settings className="w-5 h-5" />
                <span className="text-sm font-medium">设置</span>
              </button>
              <button className="w-full flex items-center justify-between py-3 px-2 rounded-lg hover:bg-white/5 transition text-gray-300 hover:text-white group">
                <div className="flex items-center gap-3">
                  <Moon className="w-5 h-5" />
                  <span className="text-sm font-medium">深色模式</span>
                </div>
                <div className="w-10 h-5 bg-red-600 rounded-full relative shadow-inner">
                  <div className="absolute right-0.5 top-0.5 w-4 h-4 bg-white rounded-full shadow"></div>
                </div>
              </button>
              <button className="w-full flex items-center justify-between py-3 px-2 rounded-lg hover:bg-white/5 transition text-gray-300 hover:text-white group">
                <div className="flex items-center gap-3">
                  <Timer className="w-5 h-5" />
                  <span className="text-sm font-medium">定时关闭</span>
                </div>
                <ChevronRight className="w-4 h-4 opacity-50 group-hover:opacity-100" />
              </button>
            </div>
          </div>

          {/* 底部操作区 */}
          <div className="p-4">
            <button className="w-full flex items-center justify-center gap-2 py-3 rounded-xl bg-white/5 hover:bg-red-500/20 text-red-400 transition">
              <LogOut className="w-4 h-4" />
              <span className="text-sm font-bold">退出登录</span>
            </button>
          </div>
        </div>

        {/* --- 顶层：主应用视图 (Main App View) 带有纯 2D 挤压 (Push) 动画 --- */}
        <div 
          className={`absolute inset-0 z-10 bg-gradient-to-b from-[#3a1515] via-[#121212] to-[#0a0a0a] transition-transform duration-300 ease-[cubic-bezier(0.32,0.72,0,1)] flex flex-col shadow-[-15px_0_40px_rgba(0,0,0,0.6)] ${
            isDrawerOpen 
              ? 'translate-x-[75%]' 
              : 'translate-x-0'
          }`}
        >
          {/* 遮罩：开启侧边栏时，主视图添加黑色半透明遮罩，点击可关闭侧边栏 */}
          {isDrawerOpen && (
            <div 
              className="absolute inset-0 z-50 bg-black/30 cursor-pointer"
              onClick={() => setIsDrawerOpen(false)}
            />
          )}

          {/* 内容滚动区域，底部预留极大空间给一体化悬浮舱 */}
          <div className="h-full overflow-y-auto pb-[220px] no-scrollbar">
            
            {/* 原版 Header 区域 (静态固定设计) */}
            <div className="px-5 pt-10 pb-4 flex justify-between items-center">
              <div className="flex items-center gap-3">
                <div 
                  onClick={() => setIsDrawerOpen(true)}
                  className="w-10 h-10 bg-red-600 rounded-full flex items-center justify-center shadow-lg shadow-red-600/30 cursor-pointer hover:scale-105 transition"
                >
                  <Disc3 className="w-6 h-6 text-white" />
                </div>
                <div>
                  <p className="text-xs text-gray-300">早上好，</p>
                  <h1 className="text-lg font-bold">哥哥</h1>
                </div>
              </div>
              <button className="relative w-10 h-10 bg-white/10 rounded-full flex items-center justify-center hover:bg-white/20 transition">
                <Bell className="w-5 h-5" />
                <span className="absolute top-2.5 right-2.5 w-2 h-2 bg-red-500 rounded-full border-2 border-[#2a1215]"></span>
              </button>
            </div>

            {/* 原版 欢迎标语 */}
            <div className="px-5 mt-2">
              <h2 className="text-4xl font-extrabold leading-tight">
                欢迎来到 <span className="text-red-500">云村</span><br />
                的音乐世界
              </h2>
            </div>

            {/* 原版 分类标签 (Filter Pills) */}
            <div className="px-5 mt-6 flex gap-3 overflow-x-auto no-scrollbar pb-2">
              {['全部', '音乐', '播客', '有声书', '直播'].map((filter, index) => (
                <button 
                  key={index}
                  className={`whitespace-nowrap px-5 py-2 rounded-full text-sm font-medium transition ${
                    index === 0 
                    ? 'bg-red-600 text-white' 
                    : 'bg-white/10 text-gray-200 hover:bg-white/20'
                  }`}
                >
                  {filter}
                </button>
              ))}
            </div>

            {/* 动态 Feed 流区块：将数据传入渲染引擎 */}
            <div className="mt-6 space-y-6">
              {isLoading ? (
                <div className="flex justify-center items-center h-40">
                  <div className="w-8 h-8 border-4 border-red-600 border-t-transparent rounded-full animate-spin"></div>
                </div>
              ) : (
                feedData.map((section) => (
                  <section key={section.id} className="w-full">
                    <div className="px-5 flex justify-between items-end mb-4">
                      <h3 className="text-xl font-bold">{section.title}</h3>
                      <button className="text-sm text-gray-400 hover:text-white transition">查看全部</button>
                    </div>
                    {renderSectionContent(section)}
                  </section>
                ))
              )}
            </div>
          </div>

          {/* 融合保留的：一体化悬浮播放器与导航栏 (Dynamic Island 式) */}
          <div className="absolute bottom-6 left-4 right-4 z-20">
            <div className="bg-[#121212]/40 backdrop-blur-2xl rounded-[2rem] p-4 flex flex-col shadow-2xl border border-white/10">
              {/* 上半部分：播放器 */}
              <div className="flex items-center gap-3 mb-2 px-1">
                <div className="w-12 h-12 rounded-xl overflow-hidden shrink-0 shadow-md">
                  <img 
                    src="https://images.unsplash.com/photo-1614613535308-eb5fbd3d2c17?w=100&q=80" 
                    alt="Now Playing" 
                    className="w-full h-full object-cover"
                  />
                </div>
                <div className="ml-1 flex-1 overflow-hidden">
                  <h4 className="font-bold text-sm text-white truncate">Igual Que Un Angel</h4>
                  <p className="text-xs text-gray-400 truncate">Kali Uchis</p>
                </div>
                <div className="flex items-center gap-4 shrink-0">
                  <button className="text-gray-300 hover:text-white transition">
                    <Cast className="w-5 h-5" />
                  </button>
                  <button className="text-white hover:scale-110 transition">
                    <Play className="w-6 h-6 fill-current" />
                  </button>
                </div>
              </div>

              {/* 播放进度条 */}
              <div className="mx-1 h-[2px] bg-white/20 rounded-full overflow-hidden mb-4 relative">
                <div className="absolute top-0 left-0 h-full bg-red-500 w-1/3 rounded-full shadow-[0_0_8px_rgba(239,68,68,0.8)]"></div>
              </div>

              {/* 下半部分：导航栏 (融合版) */}
              <div className="flex justify-between items-center px-4 pb-1">
                <button className="flex flex-col items-center gap-1 group">
                  <Home className="w-6 h-6 text-white group-hover:scale-110 transition" />
                  <span className="text-[10px] text-white font-medium">主页</span>
                </button>
                <button className="flex flex-col items-center gap-1 group">
                  <Search className="w-6 h-6 text-gray-400 group-hover:text-white group-hover:scale-110 transition" />
                  <span className="text-[10px] text-gray-400 group-hover:text-white transition">搜索</span>
                </button>
                <button className="flex flex-col items-center gap-1 group">
                  <Library className="w-6 h-6 text-gray-400 group-hover:text-white group-hover:scale-110 transition" />
                  <span className="text-[10px] text-gray-400 group-hover:text-white transition">音乐库</span>
                </button>
                <button className="flex flex-col items-center gap-1 group">
                  <PlusSquare className="w-6 h-6 text-gray-400 group-hover:text-white group-hover:scale-110 transition" />
                  <span className="text-[10px] text-gray-400 group-hover:text-white transition">创建</span>
                </button>
              </div>
            </div>
          </div>

        </div>

      </div>
      
      {/* 隐藏滚动条的全局样式 */}
      <style dangerouslySetInnerHTML={{__html: `
        .no-scrollbar::-webkit-scrollbar {
          display: none;
        }
        .no-scrollbar {
          -ms-overflow-style: none;
          scrollbar-width: none;
        }
      `}} />
    </div>
  );
};

export default App;